[project]
name = "shared"
version = "0.1.0"
description = "shared libs for packages"
readme = "README.md"
authors = [
    { name = "Stanislav M", email = "stanislav.herdt@gmail.com" }
]
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
